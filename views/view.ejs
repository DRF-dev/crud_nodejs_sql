<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View all</title>
</head>

<body>
    <ul>
        <% data.forEach(d => { %>
            <li>
                <%= d.name %>
                    <%= d.age %>ans
                        <%= d.mail %>
            </li>
            <% }) %>
    </ul>
</body>

</html>